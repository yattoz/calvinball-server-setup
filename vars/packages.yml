

debian_runtime_suite:
  - git
  - build-essential
  - curl
  - autoconf
  - bison
  - ruby
  - ruby-dev
  - bundler
  - nodejs
  - npm

debian_nokogiri_build:
  - patch
  - zlib1g-dev
  - liblzma-dev

debian_rmagick_build:
  - libmagickwand-dev
  - imagemagick
  - pngquant

debian_utilities:
  - at
  - cron

debian_web_access:
  - nginx-full
  - python3-certbot-nginx
  - cockpit

debian_packages: "{{debian_runtime_suite + debian_nokogiri_build + debian_rmagick_build + debian_utilities + debian_web_access }}"